{
    "scripts": {
        "docker-build": "docker image build -t house-points-api .",
        "docker-start": "docker container run --network=host --name=house-points-api-container --restart=on-failure -d -p=4464:4464 house-points-api",
        "start": "docker container prune; npm run docker-build; npm run docker-start",
        "restart": "docker container prune; npm run docker-build; docker container restart house-points-api-container",
        "stop": "docker container prune; docker container stop house-points-api-container"
    }
}
